# Notelikeus production audit — 2026-09-05 (Opus 5 continuation)

This report finishes the in-progress adversarial production audit. A previous agent completed
roughly the first half (P0/P1 data-loss and several P2/P3 fixes). This session spot-checked that
work, measured and fixed the interrupted performance item, acted on the confirmed-but-unfixed
table, audited the remaining security/UX/supply-chain surfaces that can be reviewed without
Docker or a device, and wrote this document.

Nothing here claims the product is perfect. Blocked suites are listed as blocked.

---

## A. Audit identity

| Field | Value |
|---|---|
| Date | 2026-09-05 |
| Repository | `c:\Users\LENOVO\AndroidStudioProjects\Notelikeus` |
| Baseline commit | `9a74868bc9ebe15bb7fab5d3aaea1480c75c5fb9` (`fix(web): stop double-exchanging the Google OAuth PKCE code`) |
| Branch | `cutover/supabase-canonical` |
| Working tree | Dirty — continuation uncommitted (E2E CI job, snapshot `note_count` guard, compact chips, encrypted session store, persisted widget token, widget settings path, dual-DataStore singleton, Glance `NEW_TASK` launch helper, deferred supabase-js, CSP Worker pin, empty-library snapshot SQL, web delete snapshot refresh). Prior audit commit: `4dcf419`. |
| Node / npm | v24.16.0 / 12.0.2 |
| Java / Gradle / Kotlin | OpenJDK 25.0.3 / Gradle 9.7.1 / Kotlin 2.4.0 (project Kotlin plugin 2.1.21) |
| Supabase CLI | 2.116.0 (prior). Docker Desktop **4.89.0** is now running (`docker-desktop` WSL 2). pgTAP and Playwright executed this continuation. |
| Android SDK | present, `adb` 1.0.41, AVD `Medium_Phone_API_36.1` (API 36) cold-booted this continuation as `emulator-5554` |
| Platform | Windows 11, PowerShell |
| Prior audits | `docs/COMPREHENSIVE_AUDIT.md`, `docs/AUDIT_REPORT.md`, `docs/AUDIT_2026-09-02.md`, `docs/FINDINGS.md` |
| Handoff consumed | `docs/AUDIT_HANDOFF_2026-09-05.md` (deleted after this report was written) |

### Evidence grades used below

| Grade | Meaning |
|---|---|
| **Executed** | Command was run in this session (or the prior session of this audit, where explicitly attributed) and produced a pass/fail. |
| **Measured** | Timed with a harness; numbers recorded. |
| **Statically reviewed** | Source read; no runtime. |
| **Blocked** | Dependency missing; the exact remaining command is named. |

---

## B. Executive verdict

**CONDITIONALLY RELEASE READY.**

That is the honest ceiling. Unit tests, lint, typecheck, the web production build, Pages artifact
and header checks, the attachments Worker suite, Android `EditorViewModel` regression tests, and
a release APK with R8 + resource shrinking all passed. The P0 library-wipe path
(OPUS-DATA-001) is fixed with tests that fail without the guard. Several P2/P3 holes from the
handoff are now fixed.

It is not **RELEASE READY**, because this environment still has not run:

- Real-browser visual QA, Lighthouse, zoom/theme sweeps
- The packaged Windows desktop app
- On-device Glance bind, App Functions while App lock is on, SQLCipher after a Keystore-invalidating credential reset
- A production custom domain in Worker `ALLOWED_ORIGINS` (none is attached yet)

pgTAP and Playwright against local Supabase **passed this continuation** (section C / M). Live staging Pages + Worker were redeployed and probed (section M).

Do not upgrade the verdict on the strength of unit tests alone.

**Do not ship until** the Block-release checklist in section N is green: device remainder, and `ALLOWED_ORIGINS` if a custom domain is attached. Hosted OPUS-DATA-006 and OPUS-PERF-004 are applied.

---

## C. Architecture map

Verified by reading current source (spot-checked against the handoff; not blindly trusted).

```
Web (React 19 + Zustand + Vite PWA)
  UI → stores (notes, auth, tombstone, settings, ui, labelRegistry)
     → noteActions
     → IndexedDB notesLocalRepository (namespaced by ownerId; guest = GUEST_OWNER_ID)
     → notesSyncService → supabaseRemoteNotesDataSource → RPC + Realtime
  Attachments → Cloudflare Worker → R2 (objectKey on the note)

Kotlin (KMP composeApp: Android + desktop)
  Compose UI → ViewModels → NoteRepositoryImpl
     → Room + SQLCipher (Android). Desktop SQLite is unencrypted (accepted, documented).
     → NoteSyncEngine → SupabaseNoteTransport → same RPCs
  Android sync: per-note WorkManager via CloudNoteSyncCoordinator
  No Kotlin delta-subscribe path (web Realtime only)

Backend (supabase/migrations)
  notes, note_tombstones, sync_meta, note_attachments
  RLS enabled; mutations through SECURITY INVOKER RPCs; sync_mutation_guard blocks direct writes
  Server sequence revision + server_updated_at (client clock cannot win a confirmed conflict)
```

**Duplicated logic that must be audited together** (still true):

| Concern | Web | Kotlin | Worker / DB |
|---|---|---|---|
| Backup import caps | `web/src/lib/backup/*` | `NoteBackupImporter.kt` | Postgres CHECK |
| Conflict resolution | `remoteMerge.ts:shouldUploadOverRemote` | `NoteSyncEngine.cloudWinsConflict` | `apply_note_change` base revision |
| Attachment object keys | `attachmentObjectKey.ts` | `AttachmentObjectKey.kt` | `workers/attachments/src/objectKey.ts` |
| Field caps | editor `maxLength` + save clamp | `EditorViewModel` clamp (this session) | `char_length` CHECKs |

Conflict helpers were re-read this session: when both sides have `serverUpdatedAt`, the higher
stamp wins; equal stamps break on client `timestamp` with a tie going to cloud; a confirmed
remote beats an unconfirmed local. Web `>` vs Kotlin cloud-wins `>=` on the remote timestamp are
equivalent. **Statically aligned.** Two-client live races remain **Blocked** (need two clients +
local stack).

---

## D. Test matrix

| Area | Command/Test | Result | Evidence/Notes |
|---|---|---|---|
| Throwaway bench test | `web/src/lib/notes/_bench.test.ts` | **PASS (removed)** | Deleted this session; it threw on purpose and would have failed `npm test`. |
| OPUS-PERF-001 measure | `node web/scripts/benchNotesContentKey.mjs` | **Executed** | See section I. 5k notes / 2KB bodies: old key **9.326 ms/call**. |
| Web lint | `npx oxlint` (web) | **PASS** | 0 errors; warnings only (pre-existing unicorn/await-in-loop style). |
| Web unit tests | `npm test` (web) | **PASS** | **53 files / 349 tests** (includes `shouldStartSupabaseAuthOnBoot`). |
| Web typecheck + prod build | `npm run build` (web) | **PASS** | This continuation. SW precache **44 entries / 921.26 KiB**. `supabase-DdI02N5Y.js` still **208.64 kB / 53.98 kB gzip**, **not** modulepreloaded from `index.html`. |
| Pages artifacts | `node web/scripts/verifyPagesArtifacts.mjs` | **PASS** | This session, after the build. |
| Security headers parity | `node cloudflare/scripts/verify-headers-parity.mjs` | **PASS** | This session; CSP now pins the project Supabase host. |
| Attachments Worker tests | `npm test` (workers/attachments) | **PASS** | 3 files / 25 tests. |
| Worker typecheck | not re-run this session | **N/A** | Prior session of this audit: PASS. |
| Kotlin EditorViewModel | `./gradlew :composeApp:testDebugUnitTest --tests com.aus.notelikeus.ui.editor.EditorViewModelTest --rerun-tasks` | **PASS** | XML: **15 tests, 0 failures, 0 skipped** (`TEST-com.aus.notelikeus.ui.editor.EditorViewModelTest.xml`). Includes the two new clamp tests. |
| Kotlin snapshot transport | `./gradlew :composeApp:desktopTest --tests com.aus.notelikeus.data.remote.SupabaseNoteTransportTest --rerun-tasks` | **PASS** | XML: **7 tests, 0 failures** including `aTruncatedSnapshotIsRejected`. |
| Kotlin full suite | prior session `desktopTest` + `testDebugUnitTest` + `androidApp:testDebugUnitTest --rerun-tasks` | **PASS (prior session)** | **684 tests, 0 failures** (composeApp android 366, desktop 301, androidApp 17). **Not re-executed in this session.** |
| Android release | `./gradlew :androidApp:assembleRelease` | **PASS** | This session after the Kotlin clamp/string edits. R8 + `lintVital` + `packageRelease`. APK `androidApp-release-unsigned.apk` **13,392,384 bytes**. `mapping.txt` produced (62,816,968 bytes). Prior session APK was 13,392,336 bytes (comment-only drift). |
| `npm audit --omit=dev` (web) | production deps | **PASS** | **0 vulnerabilities.** |
| Gradle dependency CVEs | `libs.versions.toml` read | **Statically reviewed** | Pins documented; Compose/Koin version-train comments still accurate. No scanner run. |
| Supabase pgTAP / RLS / RPC | `npm run supabase:start && reset && test` | **PASS** | Docker Desktop 4.89.0. **12 files / 81 tests**, including `notelikeus_fetch_full_snapshot.test.sql` (OPUS-DATA-006) and `notelikeus_rls_initplan.test.sql` (OPUS-PERF-004). |
| Playwright E2E | `npm run test:e2e` | **PASS** | **3/3** Chromium vs local `http://127.0.0.1:54321` (boot, signed-out gate, note create/reload/edit). |
| Android instrumented | `./gradlew :composeApp:connectedDebugAndroidTest :androidApp:connectedDebugAndroidTest` | **PASS** | AVD `Medium_Phone_API_36.1` (API 36), cold boot after snapshot Error -5. composeApp **4/4** (`PlaintextDatabaseMigratorTest`). androidApp **2/2** (`PredictiveBackTest`). Debug APK launched: sign-in screen, then guest empty library. |
| Real-browser visual / Lighthouse / zoom / themes | manual or Playwright | **BLOCKED** | No local stack; no device. Source-reviewed only. |
| Packaged Windows desktop | MSI / `composeApp:run` packaged | **BLOCKED** | Not launched. |
| Web CI gate expansion | `.github/workflows/web.yml` | **Statically reviewed** | `build` still verifies Pages artifacts + headers. New `e2e` job starts local Supabase and runs Playwright. The same Playwright suite **passed locally** this continuation; GHA has not executed the job yet. |

---

## E. Findings table

Status: **FIXED** = reproduced (or confirmed from prior session), patched, regression test where
meaningful. **OPEN** = still present. **ACCEPTED** = confirmed, deliberately not patched.
**PARTIAL** = narrowed but not eliminated.

### Prior session of this audit (carried forward; spot-checked)

| ID | Severity | Platform | Area | Problem | User Impact | Evidence | Reproduction | Proposed Fix | Status |
|---|---|---|---|---|---|---|---|---|---|
| OPUS-DATA-001 | P0 | Web | Sync | Partial Realtime snapshot mass-tombstoned the library | Silent multi-device data loss | `supabaseRemoteNotesDataSource.ts` `hasBaseline` + queue; `partialRemoteSnapshot.test.ts` (4 tests) | Failed bootstrap → reconcile → one Realtime wake tombstoned 4/5 notes | Do not emit until full snapshot; pull without baseline re-fetches snapshot | **FIXED** (prior session). Guard still present this session. |
| OPUS-WEB-001 | P1 | Web | Editor | Focus trap re-ran every keystroke | Caret stolen to toolbar | `useFocusTrap` handler ref; effect keyed on `[open, closeOnEscape]` | Type one character in body | Stabilize trap deps | **FIXED** (prior). |
| OPUS-WEB-002 | P2 | Web | Dialogs | One Escape closed every stacked dialog | Editor closed with inner dialog | Trap registry + containment | Nested dialog Escape | Innermost trap only | **FIXED** (prior). Tests in `useFocusTrap.test.ts`. |
| OPUS-A11Y-001 | P2 | Web | Drawer | Closed mobile drawer stayed tabbable | Focus into `aria-hidden` subtree | `SideDrawer.test.tsx` (4 tests) | Tab from closed drawer | `inert` + trap only when mobile overlay | **FIXED** (prior). |
| OPUS-AND-001 | P2 | Android | Share | Unbounded share-target text | Local save then permanent sync reject | `NavigationIntents.android.kt` clamps to importer caps | Share a long article | Clamp to backup caps | **FIXED** (prior). |
| OPUS-SEC-001 | P3 | Web | SW | Reminder payloads unvalidated | One bad entry killed all reminders; lock-screen text leak | `swReminderPayload.ts` | Malformed SW message | Sanitize; own title/body | **FIXED** (prior). |
| OPUS-SEC-002 | P3 | Web | Deep link | `OPEN_NOTE` postMessage skipped id regex | Open arbitrary string as note id | `App.tsx` | SW message with non-digits | Same `/^\d+$/` as `?note=` | **FIXED** (prior). |
| OPUS-DATA-002 | P3 | Web | Backup | No root label cap | Hostile 10 MB file → million-entry Set | `MAX_BACKUP_LABELS` in `constants.ts` | Label-only backup | Cap at 2,000 | **FIXED** (prior). |
| OPUS-DOC-001 | P2 | Store | Play | Data safety still said Firebase / no photos | Play Console contradiction | `store/DATA_SAFETY.md` | Read the form source | Rewrite for Supabase + R2 | **FIXED** (prior). |

### This session

| ID | Severity | Platform | Area | Problem | User Impact | Evidence | Reproduction | Proposed Fix | Status |
|---|---|---|---|---|---|---|---|---|---|
| OPUS-PERF-001 | P2 | Web | Render | `notesContentKey` copied the library, concatenated **full title+body** for every note, sorted, joined — on **every** `useNotes()` / `useNoteLabels()` render | At 5k×2KB notes: **9.3 ms/call** before paint; 1k notes 1.67 ms. Two hooks + store equality compounded it. | `web/scripts/benchNotesContentKey.mjs` (this session). Old vs new: 5k same-ref equality **15.987 ms → 0.000 ms**; reversed **0.424 ms**. | Open main grid with a large library and type in search (re-renders `useNotes`) | Stop calling the giant key on render; compare notes by identity then by field. Cheap `notesContentEqual` for `setNotes`. | **FIXED.** `useNotes` / `useNoteLabels` depend on the store’s stable `notes` reference. `notesContentEqual` is identity + pairwise. Tests in `noteEquality.test.ts`. |
| OPUS-DATA-003 | P2 | Both | Editor | Content not clamped → `apply_note_change` fails forever | Unsyncable note after a large paste | Web already had `maxLength` on title **and** body plus save-path `slice` in `useNoteEditor.ts` (handoff was stale on web). Android `onTitleChange` / `onContentValueChange` clamped nothing. | Paste 100,001 chars on Android | Clamp to importer caps; snackbar when shortened | **FIXED** on Android/desktop ViewModel (clamp + `note_truncated_to_sync_limit` snackbar). Web already clamped. Tests: `EditorViewModelTest` 15/15. Product choice: clamp rather than leave unsyncable text. |
| OPUS-DATA-004 | P2 | Web | Auth | AUD2-08: `enterGuestMode` was `set({ guestMode: true })` only. Notes are IDB-namespaced, but labels/tombstones live in un-namespaced `localStorage`, and in-memory notes flashed until guest bootstrap. | Prior account label names (and briefly notes) visible in guest | `authStore.ts` now calls `clearLocalUserData()`. `bootstrap.test.ts` guest case. | Set labels/tombstones, `enterGuestMode()`, inspect stores | Clear user stores on guest entry (IDB of the prior owner is **not** wiped — same as sign-out) | **FIXED.** |
| OPUS-A11Y-002 | P3 | Web | Filters / reminders | Filter chips had no `aria-pressed`; `datetime-local` inputs unlabelled | SR users cannot hear selected label or reminder field | `FilterRow.test.tsx`; `EditorOptionsSheet.tsx`; `ReminderPickerDialog.tsx` | Inspect chips / reminder picker | `aria-pressed` on toggle chips; `aria-label` on datetime | **FIXED.** |
| OPUS-A11Y-003 | POLISH | Web | Dialogs | `aria-label` duplicated a visible `<h4>` with no `h1–h3` ancestor | Name computed twice; heading outline skipped | `ModalDialog.tsx` `aria-labelledby`; Confirm/SignOut/Link/Privacy/Reminder/delete-confirm now `<h2 id>` | Open any confirm dialog | `aria-labelledby` + `h2` | **FIXED** (horizontal sweep of dialog headings). |
| OPUS-A11Y-005 | P3 | Web | Motion | No `prefers-reduced-motion` anywhere in `web/src` | Vestibular users got 200–300 ms fades/zooms anyway | `globals.css` media query added this session | OS reduce-motion + open a dialog | Global duration collapse | **FIXED** in CSS. **Not** verified in a browser. Compose motion not audited on device. |
| OPUS-SEC-003 | P3 | Worker | CORS | Any `https://*.pages.dev` origin allowed | Any Pages tenant could be an allowed Origin (still needed a bearer token; no credentials) | `cors.ts` + `cors.test.ts` now reject `https://evil.pages.dev` | `Origin: https://evil.pages.dev` | Allow only `notelikeus-dev.pages.dev` and `*.notelikeus-dev.pages.dev`; extras via `ALLOWED_ORIGINS` | **FIXED.** Live Worker `51741604-9ecc-4b32-96ed-3b85d7ff86ad`: `notelikeus-dev.pages.dev` and `dfd73f90.notelikeus-dev.pages.dev` echoed; `evil.pages.dev` / `notelike.web.app` / `notelikeus.com` no Allow-Origin. Production custom domain must be in `ALLOWED_ORIGINS`. |
| OPUS-SEC-004 | P3 | Web | CSP | `connect-src` allowed any Supabase project and any Worker | XSS exfil breadth | Public `_headers` pins Supabase only. Vite `pinAttachmentsCspPlugin` injects `VITE_ATTACHMENTS_WORKER_URL` origin into `dist/_headers`. CI-like build: no `*.workers.dev`. Staging URL build: `https://notelikeus-attachments.error-endpoint.workers.dev` after the wss host. | `npm run build`; rebuild with Worker URL | Pin hosts from env; forbid platform wildcards | **FIXED.** Live `https://notelikeus-dev.pages.dev/` CSP pins `cqydlidescvmpfviwncf.supabase.co` + `https://notelikeus-attachments.error-endpoint.workers.dev`. No `*.supabase.co` / `*.workers.dev`. |
| OPUS-SEC-005 | P3 | Worker | Cache | Authenticated GET had no `Cache-Control: private, no-store` | Shared-cache defence-in-depth | `getAttachment` + worker test | GET attachment | Add header | **FIXED.** |
| OPUS-CI-001 | P2 | CI | Web | `web.yml` did not run Pages artifact/header verification; Playwright was local-only | Suites existed only in path-filtered `cloudflare-pages.yml`; E2E needed Docker | `web.yml` `build` job still verifies artifacts/headers; new `e2e` job starts local Supabase and runs `npm run test:e2e` | Push a headers or Playwright regression | Add scripts + a Docker E2E job | **FIXED in workflow source.** Local Playwright **PASS** 3/3 this continuation. GHA has not executed the new job yet. |
| OPUS-PERF-003 | P3 | Web/PWA | Fonts | SW precached 28 Inter subset files (~331 KB of a 1,057 KB precache) | Guest and Latin-only users paid for Cyrillic/Greek/Vietnamese bytes up front | This session build: precache **34 entries / 915.04 KiB**. `globPatterns` latin `woff2` only; SW `cacheFirstFont` for other `.woff2`. | `npm run build` + SW manifest | Precache latin*; runtime CacheFirst for the rest | **FIXED** (measured). Offline non-Latin: first load of that subset after install needs network once, then cached. |
| OPUS-PWA-001 | P3 | Web | Manifest | F45 claimed icon `sizes` were 1024×1024; `vite.config.ts` and `public/manifest.webmanifest` still said 192/512 | Browser mis-ranks PWA icons | Dist this continuation: `"sizes":"1024x1024"` plus favicon `"sizes":"any"` | `web/dist/manifest.webmanifest` | Declare actual pixel size | **FIXED.** Favicon `sizes: any` is in source and in the measured `web/dist/manifest.webmanifest`. |
| OPUS-HOUSE-001 | POLISH | Repo | Debris | `_oauth_smoke_tmp.mjs`, `firestore-debug.log` (root + `web/`), stale Firebase comments in workflows | Confusion; log noise | Deleted untracked leftovers; comments in `android.yml` / `desktop.yml` | `git status` | Delete / reword | **FIXED** for debris. `note_count` is now a truncation guard (OPUS-DATA-005). |
| OPUS-PERF-002 | P3 | Web | Boot | supabase-js 208.65 kB / 53.98 kB gzip on the critical path for guests | Slower TTI for never-signed-in visitors | Prod `index.html` no longer modulepreloads `supabase-*.js`. Chunk still **208.64 / 53.98 gzip**, loaded on sign-in / session restore / OAuth `?code=` | First visit with empty `notelikeus-had-session` | Defer until sign-in | **FIXED.** Live `https://notelikeus-dev.pages.dev/` index does not mention or modulepreload `supabase-*.js`. Gate/guest boot skips supabase-js. Returning users and Google redirect still load it immediately. |
| OPUS-SEC-006 | P3 | Worker | Abuse | No per-user quota / rate limit | Authenticated user can PUT unlimited 10 MB objects | Worker source | Authed upload loop | Ops quota | **ACCEPTED** (ops). |
| OPUS-WEB-003 | P3 | Web | Editor | `useNoteEditor` still returns a fresh object every render | Downstream memo churn; user-visible trap bug already fixed | `useNoteEditor.ts` `return { state, …inline lambdas }` | Profile re-renders | Split state vs `useCallback` actions | **ACCEPTED.** Object **must** change when `state` changes. Symptom of OPUS-WEB-001 is fixed. |
| OPUS-AND-002 | P3 | Android | Widget | `InternalNavigationToken` was process-ephemeral | Widget / reminder tap no-ops after force-stop until the next in-process mutation | `MODE_PRIVATE` prefs `notelikeus_internal_nav`; `commit()` on first write | Force-stop, then an internal intent with the stored token | Persist the token so `matches()` still holds after process death | **FIXED.** Unit + emulator. Pixel 7 live tap is OPUS-AND-005. |
| OPUS-A11Y-004 | POLISH | Web | Touch | Compact filter chips used `min-h-9` (36px) vs 44px chrome / 48dp Material | Harder to tap on phones | `FilterRow.tsx` no longer overrides `.filter-chip` `min-h-11` | 320px width, thumb | Drop the compact height override | **FIXED.** Compact chips keep smaller padding/text but inherit 44px min-height. |
| OPUS-DATA-005 | P2 | Both | Sync | `fetch_full_snapshot.note_count` was unused, so a truncated `jsonb_agg` could still look like a complete library | Same mass-tombstone class as OPUS-DATA-001 if the payload was cut | `supabaseSyncEngine.ts` / `SupabaseNoteTransport.kt` throw when `note_count !== notes.length` | Snapshot with 2 notes and `note_count: 5` | Reject before `hasBaseline` / revision-map clear | **FIXED.** Web: `partialRemoteSnapshot.test.ts` (5 tests). Kotlin: `SupabaseNoteTransportTest` 7/7 including `aTruncatedSnapshotIsRejected`. |
| OPUS-SEC-007 | P3 | Android | Session | GoTrue tokens lived in deprecated EncryptedSharedPreferences (or a plaintext fallback if ESP failed) | Tokens on a dying API; failover was unencrypted | `AndroidSupabaseSessionPersistence` now uses `PassphraseFileCodec` + Keystore alias `notelikeus_supabase_session_aes`; one-shot ESP/fallback migrate | Sign-in, kill process, confirm still signed in | Same store as the DB passphrase | **FIXED.** Tests 5/5. Not re-verified with a live Keystore after a credential reset. |
| OPUS-AND-003 | P2 | Android | Widget | `preferencesDataStore(name = "settings.preferences_pb")` appends `.preferences_pb` again, so Glance read a second empty store | App lock / theme written by Koin never reached the widget | `SETTINGS_DATASTORE_NAME = "settings"` so both paths use `files/datastore/settings.preferences_pb` | Bind widget, enable app lock, confirm empty | Same filename as `createDataStore` | **FIXED** (filename). Pixel 7 bound Glance `AppWidgetId` **312**. Live widget showed notes once the process was up. Same-file dual factory was OPUS-AND-004. |
| OPUS-AND-004 | P1 | Android | Widget / App Functions | Koin `createDataStore` and Glance `Context.settingsDataStore` were two DataStore instances on `settings.preferences_pb` | After AND-003 aligned the path, App Functions crashed: `multiple DataStores active for the same file` (code 3000) | Pixel 7 `31071FDH2007WT`, widget id 312 on screen, then `listNotes` | Bind widget, invoke `listNotes` | Koin injects the `settingsDataStore` singleton | **FIXED.** Reinstall + `listNotes` with the widget on-screen **PASS**. |
| OPUS-AND-005 | P2 | Android | Widget | Glance `actionStartActivity` intents lacked `FLAG_ACTIVITY_NEW_TASK \| FLAG_ACTIVITY_SINGLE_TOP` and a per-note data URI | After force-stop, a widget tap stayed on the launcher (or was easy to miss: the 3×2 sits left of center) | `widgetMainActivityIntent`: flags `0x30000000`, `notelikeus://editor/{id}`, token, extras. Reminder uses the same helper. | Force-stop, tap a note row on AppWidgetId 312 | Match `ReminderReceiver` launch flags | **FIXED.** Pixel 7: `ActivityTaskManager START` `dat=notelikeus://editor/{id} flg=0x30000000` `realCallingUid` launcher → `MainActivity`. Gate showed **Sign in to continue** (session not restored). |
| OPUS-AND-006 | P2 | Android | Gate | `onNewIntent` always overwrote `pendingNoteId` / `pendingCreateNote` | Launcher icon while the sign-in gate is up dropped a widget/share payload queued behind the gate | Ignore intents with no share / token payload | Widget → gate → Home → launcher icon → sign in | Keep the queue until a real navigation intent arrives | **FIXED** in source (not re-installed on the Pixel after AND-005). |
| OPUS-DATA-006 | P1 | Both | Sync | `fetch_full_snapshot` outer `FROM notes` returned **SQL NULL** when the owner had zero notes, so tombstones were dropped | A second device could re-upload deleted notes | Migration `20260906000000_fetch_full_snapshot_scalar.sql` builds the object from scalar subqueries. Clients throw on null / missing `note_count`. | Delete last note; snapshot must include tombstones | Always return JSON; clients fail closed | **FIXED.** Local pgTAP **PASS**. Hosted `notelikeus-staging` (`cqydlidescvmpfviwncf`) history includes `20260906000000 fetch_full_snapshot_scalar`; function body verified. |
| OPUS-DATA-007 | P2 | Web | Sync | `deleteNote` returned without `apply_note_delete` when no cached base revision | Cloud copy survived; note reappeared on the next device that synced | Snapshot refresh then RPC, matching Kotlin `deleteNotes` | Delete after a cold IDB / before baseline | Fetch snapshot once, skip RPC only if still unknown | **FIXED.** `supabaseDeleteNote.test.ts` 6/6. |
| OPUS-PERF-004 | P3 | DB | RLS | Hosted advisor `auth_rls_initplan`: 16 policies called `auth.uid()` per row | Extra JWT/`current_setting` work at library scale; no isolation change | `get_advisors` performance on `cqydlidescvmpfviwncf` listed all notes / tombstones / sync_meta / note_attachments policies | Advisor after DDL | Wrap as `(SELECT auth.uid())` | **FIXED.** Migration `20260906010000_rls_initplan.sql`. Local pgTAP **12 files / 81 tests**. Hosted history `20260906010000 rls_initplan`; `pg_get_expr` shows `( SELECT auth.uid() AS uid)`. Performance advisors after apply: **empty**. |
| OPUS-DATA-008 | P2 | Web | Sync | `uploadAllNotes` upserted every local note with no snapshot / `shouldUploadOverRemote` check (Kotlin already had both) | Signed-in backup import could fail on a stale existing id, or push unconfirmed local over a newer cloud row if a snapshot failed open | `supabaseRemoteNotesDataSource.ts`; `supabaseUploadAllNotes.test.ts` (6) | Import an older backup while cloud has a confirmed copy of the same id; empty snapshot after a prior sync | Fetch snapshot; refuse suspect empty cloud; skip when remote wins | **FIXED.** New imported ids still upload. Existing cloud-newer rows are skipped. |

---

## F. Security assessment

**Grade: statically reviewed; local pgTAP PASS; hosted advisors re-checked.**

### What still holds (re-read / unchanged from prior session of this audit)

- Five tables: RLS + `owner_id = (SELECT auth.uid())` (OPUS-PERF-004). RPCs are `SECURITY INVOKER`, `search_path = public`, owner from `auth.uid()` never from a parameter.
- `sync_mutation_guard` flag is transaction-local, set only inside RPCs.
- `register_note_attachment` requires `object_key == expected_attachment_object_key(auth.uid(), …)`.
- `REVOKE … FROM PUBLIC, anon` pattern in later migrations — new function signatures still get default grants; anyone adding RPCs must revoke.
- Worker: token verified against `/auth/v1/user`; R2 key derived from auth id; MIME allowlist; size cap on `Content-Length` and streaming read.
- Web `src/`: no `innerHTML` / `dangerouslySetInnerHTML` / `eval` / `new Function` (grep this session).
- Markdown links: `toSafeHref`. SW `postMessage` origin-checked. Android deep links need the internal token.
- No `service_role` in client bundles (prior session; not re-unpacked this session).

### SQLCipher / Keystore (this session, static + existing unit tests)

`DatabaseKeyManager` holds a 32-byte `SecureRandom` passphrase. Primary wrap: AES-GCM file under
`filesDir`, key in AndroidKeyStore. Legacy EncryptedSharedPreferences is migrated once then
cleared. Undecryptable file is **moved aside**, not deleted. F26 recovery: if **encryption** fails
(invalidated Keystore key), the alias is deleted and retried **once**; publish/rename failure must
not delete a healthy key. Tests: `DatabaseKeyManagerRecoveryTest`, `DatabaseKeyManagerTest`
(prior suite). **Not** re-run this session.

There is **no live passphrase rotation** while a populated DB exists (rekey would be a migration).
Uninstall + reinstall generates a new passphrase; the old ciphertext is unreadable — fail-closed.
**Corrupted-ciphertext recovery on device** is still **Blocked** (instrumented / real Keystore).

Desktop DB: unencrypted SQLite, accepted. Desktop session: `.supabase-session` wrapped with DPAPI
(`DesktopSupabaseSessionPersistence`). Decrypt/`UnsatisfiedLinkError` caught as `Throwable`;
undecryptable file is kept (F27). Fail-closed to signed-out. **Not** exercised in a packaged MSI
this session.

Android session: `AndroidSupabaseSessionPersistence` now writes `supabase_session.enc` via
`PassphraseFileCodec` and a dedicated AndroidKeyStore alias (`notelikeus_supabase_session_aes`).
Legacy EncryptedSharedPreferences / plaintext fallback is migrated once then cleared. Undecryptable
file → signed-out, file kept (same fail-closed as desktop DPAPI). Tests:
`AndroidSupabaseSessionPersistenceTest` **5/5**. ESP types remain only on the one-time migration
helpers (same pattern as `DatabaseKeyManager`).

### Supply chain

- GitHub Actions: `checkout`, `setup-node`, `setup-java`, `cache`, `upload-artifact`,
  `android-emulator-runner`, `wrangler-action` are **SHA-pinned** with version comments.
  Default `permissions: contents: read`. Release job is `contents: write` + `actions: read` and
  only on `v*` tags.
- `web.yml` now also requires Pages artifact + header scripts (OPUS-CI-001 partial).
- `npm audit --omit=dev` in `web/`: **0 vulnerabilities** (executed).
- No OSV/Gradle plugin scan executed.

### Residual security

| Item | Severity | Notes |
|---|---|---|
| Attachment Worker quota | P3 | Ops (OPUS-SEC-006). |
| Pages preview CORS | mitigated | Project host only; custom prod domain needs `ALLOWED_ORIGINS`. |
| Attachments CSP vs Pages secret | ops | Dist CSP origin comes from `VITE_ATTACHMENTS_WORKER_URL`. A mismatched or empty Pages secret either blocks uploads (empty) or allows only the wrong host. |

---

## G. Data-integrity assessment

**Grade: unit-tested on clients; server suite blocked.**

| Risk | Verdict |
|---|---|
| Empty snapshot wipes UI (AUD2-01) | Guard still in `notesSyncService.ts` (`live.length === 0 && storeHoldsLiveNotes()`). |
| Partial snapshot wipe (OPUS-DATA-001) | Fixed prior session; tests still in tree. |
| Empty-library SQL NULL drops tombstones (OPUS-DATA-006) | Migration + clients fail closed. Local pgTAP **PASS**. Hosted `cqydlidescvmpfviwncf` **applied**. |
| Web delete skipped RPC without a cached revision (OPUS-DATA-007) | Snapshot refresh then `apply_note_delete`, same as Kotlin. |
| Share-target oversize (AUD2-04) | Clamped prior session. |
| Editor oversize (OPUS-DATA-003) | Web already clamped; Kotlin now clamps to the same caps and snacks. |
| Guest label/tombstone leak (AUD2-08 / OPUS-DATA-004) | `enterGuestMode` now `clearLocalUserData()`. IndexedDB of the previous **signed-in** owner is not wiped (offline edits survive re-login). Guest IDB is a separate owner. |
| Conflict / clock manipulation | Server revision + `serverUpdatedAt`. Client clock only ties or unconfirmed-vs-unconfirmed. Web `uploadAllNotes` now uses the same `shouldUploadOverRemote` helper as merge (OPUS-DATA-008). **Live two-client matrix Blocked.** |
| Tombstone TTL | `TOMBSTONE_TTL_MS = 180 days` (`tombstones.ts`). Expiry prune exists on the store. Server TTL behaviour **Blocked** (pgTAP). |
| Backup vs newer cloud | Importer clamps; `shouldUploadOverRemote` prefers confirmed remote over unconfirmed import. **Live import-vs-cloud Blocked.** |
| Account switch | `clearLocalUserDataForAccountSwitch` wipes prior IDB namespace. |

---

## H. Screen-by-screen UX table

**Grade: statically reviewed from source. Not executed in a browser or on a device.**

Responsive (320–1920), zoom 80–200%, theme contrast, and animation timing were **not** measured
in a real viewport. Compose `NotePaletteContrastTest` / `NoteContentColorTest` exist in the
desktop suite (prior session) and encode contrast math for note colors.

### Web

| Screen | Empty | Loading | Error | Offline | A11y / motion | Notes |
|---|---|---|---|---|---|---|
| `AuthScreen` | Sign-in + guest | BootGate loading | Boot failure copy | N/A (local guest works) | Overlay trap; `CHROME_FOCUS`; Google button uses theme tokens (F42) | Guest now clears labels/tombstones |
| `MainScreen` | `NotesEmptyState` only when **not** loading | `NotesLoadingGrid` (F13 still holds: loading ≠ empty overlay) | Retry uses `text-red-500` / `brand-primary` (F40) | `OfflineBanner` | Filter chips `aria-pressed`; compact chips inherit 44px `min-h-11` (OPUS-A11Y-004) | Selection bar; trash banner |
| `EditorScreen` | New note placeholders | Sync from store | save failure toast (web) | Local IDB still writes | Overlay + float traps (F36); body `aria-label`; `maxLength` title+body | Escape flushes save (F28) |
| `LabelsScreen` | Empty list | N/A | N/A | Local | Trap + dialog (F37); labelled edit input (F34) | |
| `ProfileSheet` | N/A | N/A | Import/export toasts | Backup still local | Trap; `CHROME_FOCUS` rows; min-h 3.5rem | Theme base / accent / AMOLED |
| Dialogs | N/A | N/A | N/A | N/A | `h2` + `aria-labelledby` this session | Sign-out, delete, privacy, link, reminder |
| PWA | N/A | SW `autoUpdate` + skipWaiting | Registration catch | Precache 915 KiB + font runtime cache | Install prompt component | Icon sizes 1024×1024 in this session’s dist |

### Kotlin (Android + desktop, shared Compose)

| Screen | Notes |
|---|---|
| `MainScreen` / `MainScaffold` | Snackbars, undo, selection, filters. Desktop editor is a **separate OS window** (`EditorWindowLauncher`), not an enlarged phone layout. **Packaged desktop UX Blocked.** |
| `EditorScreen` | Clamp + truncation snackbar this session. Toolbar `canFocus = false` (F44). |
| `LabelsScreen` | Shared. |
| Widget / share / App Functions | Token persisted (OPUS-AND-002). Share clamped. OS `listNotes`/`searchNotes` returned `SHARE-AUDIT-TITLE`. Lock-gated OS invoke not run (no PIN). Widget settings file aligned with the app store (OPUS-AND-003). |

### Design system (from tokens, not a visual sweep)

Web `globals.css` `@theme`: radius `--radius-note: 18px`, `--radius-sheet: 16px`, type scale
overline/title/body, `--spacing-note-gap: 14px`, filter chips `min-h-11` (44px) including compact.
Kotlin `Spacing.kt`: `touchTarget = 48.dp`. Themes: `system` / `light` / `dark` +
accent `neutral|blue|green` + `amoled` (Kotlin `Theme.kt` comments match web `settingsStore.ts`).

---

## I. Performance results

### OPUS-PERF-001 (executed, this session)

Harness: `node web/scripts/benchNotesContentKey.mjs`. Synthetic libraries of 100 / 1000 / 5000
notes with 2,000-character bodies. Warm-up call discarded. Windows, Node 24.16.0.

| Case | n=100 | n=1000 | n=5000 |
|---|---|---|---|
| OLD `notesContentKey` (2KB bodies) | 0.379 ms | 1.674 ms | **9.326 ms** |
| OLD `notesContentEqual` identical clone | 0.369 ms | 3.193 ms | **15.987 ms** |
| NEW `notesContentEqual` same reference | 0.001 ms | 0.000 ms | **0.000 ms** |
| NEW same-order distinct arrays | 0.003 ms | 0.002 ms | 0.012 ms |
| NEW reversed order | 0.021 ms | 0.082 ms | 0.424 ms |

**Fix:** do not build a giant content string on render. The notes store already preserves the
`notes` array reference unless content changed, so `useMemo(..., [notes, filters])` is correct.
`setNotes` still uses `notesContentEqual`, now O(n) field compares with an identity fast path.

**Not measured:** real Chrome FPS, INP, or 5k-note IndexedDB hydrate.

### Bundle / PWA (executed, this session `npm run build`)

| Asset | Raw | gzip |
|---|---|---|
| `supabase-DdI02N5Y.js` | 208.64 kB | 53.98 kB |
| `react-vendor-*.js` | 189.57 kB | 59.60 kB |
| `index-CKH1xaTd.js` | 114.34 kB | 34.14 kB |
| SW precache | 44 entries | **921.26 KiB** |

OPUS-PERF-002 (defer supabase-js) **done**: `dist/index.html` modulepreloads env/bootstrap/react, not `supabase-*.js`. The chunk is fetched when the user signs in, a session hint exists, or the Google redirect brings `?code=`.

Kotlin cold start / SQLCipher init / LazyColumn: **Blocked** (no device). Recomposition: not
profiled. `NoteQueryPerformanceTest` exists in desktop suite (prior session).

---

## J. Historical-findings revalidation

### `docs/AUDIT_2026-09-02.md` AUD2-01 … AUD2-14

| ID | This session |
|---|---|
| AUD2-01 empty snapshot | **Still fixed** (`storeHoldsLiveNotes`). Partial variant was OPUS-DATA-001 (fixed). |
| AUD2-02 / AUD2-03 | **Obsolete** after Supabase base-revision (prior session). Not re-litigated. |
| AUD2-04 share size | **Fixed** OPUS-AND-001. |
| AUD2-05 widget token | **Fixed** OPUS-AND-002 (prefs persist) + OPUS-AND-005 (Pixel 7 Glance tap after force-stop). |
| AUD2-06 App Functions on device | **OS invoke PASS** (`cmd app_function execute-app-function` `listNotes` / `searchNotes` returned id 1 `SHARE-AUDIT-TITLE`). Lock-gated OS invoke still **not run** (enabling App lock needs a credential; PIN was cleared after a prior crash). Unit: `NoteAppFunctionsTest` refuse-while-locked. |
| AUD2-07 Firestore cache | **Obsolete.** Account switch clears owner IDB. |
| AUD2-08 guest labels/tombstones | **Confirmed, then fixed** OPUS-DATA-004. |
| AUD2-09 SideDrawer | **Fixed** OPUS-A11Y-001. |
| AUD2-10 ARIA remainder | Filter chips + datetime **fixed** OPUS-A11Y-002. |
| AUD2-11 JSON depth | **Accepted** (prior). |
| AUD2-12–14 | **Fixed** as OPUS-DATA-002, SEC-001, SEC-002. |

### `docs/FINDINGS.md` F1–F45 (spot-check of requested IDs)

| ID | Source check this session |
|---|---|
| F10 reorder SR | **Still present:** `customActions` Move up/down in `NoteStaggeredGrid.kt`; `NoteReorderSemanticsTest`. |
| F12 drawer current | **Still present:** `aria-current` on `SideDrawer.tsx`. |
| F13 empty vs loading | **Still present:** `isLoading ? NotesLoadingGrid : empty`. |
| F28 Escape save | **Still present:** editor shortcut `allowInInputs: true` → `handleBack` → `flushSave`. |
| F29 D14 headers | **Still present:** `buildBoardItems` omits date headers on search and manual. |
| F36 editor trap | **Still present:** `needsOverlayTrap` + `overlayPanelRef`. |
| F37 labels trap | **Still present:** `LabelsScreen` `useFocusTrap`. |
| F38 nested Escape | **Still present:** `closeOnEscape={!confirmDelete}`. |
| F39 sort during search | **Still present:** `sortDisabled={searchQuery.trim().length > 0}`. |
| F40 error Retry | **Still present:** theme tokens, not `text-white`. |
| F41 checklist names | **Still present:** `Remove ${text}` / `Remove empty item`. |
| F42 auth trap / Google theme | **Still present:** trap on `AuthScreen`; theme classes. |
| F43 focus rings | **Still present:** `CHROME_FOCUS` across chrome. |
| F44 format buttons | **Still present:** toolbar `pointerdown` + `setContentFocused(true)`. |
| F45 PWA sizes | **Had regressed** in `vite.config.ts` / `public/manifest.webmanifest` → **fixed this session**. |
| F1–F9, F11, F15–F27, F30–F35 | Marked FIXED/CLOSED in FINDINGS.md; **not** each re-executed. F2 `isLocked` vestige may still exist on Room; do not drop the column in isolation. F26/F27 security paths still in source (see §F). |

---

## K. Changes made (this audit, uncommitted)

**Prior session of this audit (still in the dirty tree):** remote snapshot guard; focus trap;
SideDrawer a11y; SW reminder sanitization; `OPEN_NOTE` id check; backup label cap; Android share
clamp; Data safety / Play Console / App Functions comments.

**This session:**

- Deleted `web/src/lib/notes/_bench.test.ts`. Kept `web/scripts/benchNotesContentKey.mjs`.
- Deleted untracked `web/_oauth_smoke_tmp.mjs`, `firestore-debug.log`, `web/firestore-debug.log`.
- `noteEquality.ts` / `useNotes.ts` / `useNoteLabels.ts` — OPUS-PERF-001.
- `authStore.ts` guest isolation — OPUS-DATA-004.
- Filter chips, datetime labels, dialog headings — OPUS-A11Y-002/003.
- `prefers-reduced-motion` in `globals.css`.
- Worker CORS pin, `Cache-Control: private, no-store`.
- CSP Supabase host pin; header tests / verify script.
- `web.yml` artifact + header gates; Firebase comments in android/desktop workflows.
- PWA latin font precache + runtime woff2 cache; manifest 1024×1024 sizes.
- Kotlin editor clamp + truncation snackbar + tests.
- `vite.config.ts` favicon `sizes: any` (present in measured `web/dist/manifest.webmanifest`).
- CSP Worker pin from `VITE_ATTACHMENTS_WORKER_URL`; public `_headers` has no `*.workers.dev`.

---

## L. Tests added

| Test | What it pins |
|---|---|
| `web/src/lib/notes/partialRemoteSnapshot.test.ts` | OPUS-DATA-001 (prior session) |
| `web/src/hooks/useFocusTrap.test.ts` extras | OPUS-WEB-002 (prior) |
| `web/src/components/layout/SideDrawer.test.tsx` | OPUS-A11Y-001 (prior) |
| `web/src/lib/reminders/swReminderPayload.test.ts` | OPUS-SEC-001 (prior) |
| `importBackup.test.ts` extras | OPUS-DATA-002 (prior) |
| `NavigationIntentsTest.kt` extras | OPUS-AND-001 + OPUS-AND-002 process-death + OPUS-AND-005 widget intent / URI fallback |
| `noteEquality.test.ts` identity / same-order refs | OPUS-PERF-001 |
| `FilterRow.test.tsx` (2) | OPUS-A11Y-002 |
| `bootstrap.test.ts` `enterGuestMode` | OPUS-DATA-004 |
| `cors.test.ts` rejects `evil.pages.dev` | OPUS-SEC-003 |
| Worker GET `Cache-Control` assertion | OPUS-SEC-005 |
| `EditorViewModelTest` title + content clamp (2) | OPUS-DATA-003 |
| `partialRemoteSnapshot.test.ts` `note_count` mismatch | OPUS-DATA-005 |
| `SupabaseNoteTransportTest.aTruncatedSnapshotIsRejected` | OPUS-DATA-005 |
| `AndroidSupabaseSessionPersistenceTest` (5) | OPUS-SEC-007 |
| `SettingsDataStorePathTest` | OPUS-AND-003 widget/app settings file |
| `supabaseAuthBoot.test.ts` | OPUS-PERF-002 skip supabase-js on first visit |
| `attachmentsConnectSrc.test.ts` (7) | OPUS-SEC-004 pin / reject wildcard / loopback http |
| `partialRemoteSnapshot.test.ts` null / missing `note_count` | OPUS-DATA-006 fail-closed clients |
| `SupabaseNoteTransportTest.aSnapshotWithoutNoteCountIsRejected` | OPUS-DATA-006 |
| `notelikeus_fetch_full_snapshot.test.sql` | OPUS-DATA-006 empty library + tombstone-only snapshot — **PASS** in pgTAP 12/81 |
| `supabaseDeleteNote.test.ts` snapshot refresh (2) | OPUS-DATA-007 delete without cached revision |
| `notelikeus_rls_initplan.test.sql` | OPUS-PERF-004 `(SELECT auth.uid())` on all owner policies |
| `supabaseUploadAllNotes.test.ts` (6) | OPUS-DATA-008 cloud-wins + suspect empty snapshot |

**Executed this continuation:** web **55 files / 366 tests**. `SupabaseNoteTransportTest` **8/8**. Instrumented 4+2 on `Medium_Phone_API_36.1`. Prod build: `supabase-*.js` is **not** modulepreloaded from `index.html`. Favicon `sizes: any` is in `web/dist/manifest.webmanifest`. CSP: CI-like `dist/_headers` has no `*.workers.dev`; with staging Worker URL, connect-src includes `https://notelikeus-attachments.error-endpoint.workers.dev`. Share-target smoke: `SHARE-AUDIT-TITLE` / `SharedAuditBody` opened the editor. Widget token after force-stop: persisted prefs + `noteId=1` opened that note; forged token did not. App Functions OS: `listNotes` and `searchNotes` returned id 1. Prior session of this audit: web 345, worker 25, EditorViewModel 15, headers, Pages artifacts, release APK.

---

## M. Remaining manual validation

Exact checklist — not prose. Each item is **not done** in this environment unless marked otherwise.

- [x] `npm run supabase:start && npm run supabase:reset && npm run supabase:test` — **PASS** 2026-09-06: **12 files / 81 tests**, including `notelikeus_fetch_full_snapshot.test.sql` (OPUS-DATA-006) and `notelikeus_rls_initplan.test.sql` (OPUS-PERF-004)
- [x] `cd web && npm run test:e2e` — **PASS** 2026-09-06: **3/3** Chromium (`app-boot` splash + signed-out gate; `note-lifecycle` create/reload/edit against local Auth + `apply_note_change`)
- [ ] Two-browser conflict: edit same note offline vs online; delete vs edit; long-absence reconnect
- [x] Import backup that is older than cloud; confirm cloud wins — **PASS** (unit, OPUS-DATA-008): `uploadAllNotes` skips a confirmed remote when local has no `serverUpdatedAt`; still uploads a new imported id. Live file-picker import not run in a browser.
- [x] Clock-skewed client cannot overwrite a higher server revision — **PASS** (unit): `shouldUploadOverRemote` + `uploadAllNotes` prefer `serverUpdatedAt`. RPC `apply_note_change` still rejects a stale `p_base_revision`. Live two-client matrix not run.
- [ ] Tombstone TTL / expiry against Postgres
- [x] `./gradlew :composeApp:connectedDebugAndroidTest :androidApp:connectedDebugAndroidTest` — **PASS** 4+2 on `Medium_Phone_API_36.1` (API 36), 2026-09-06
- [x] Device share-target ordinary text — **PASS** on `Medium_Phone_API_36.1`: `ACTION_SEND` opened the editor with `SHARE-AUDIT-TITLE` / `SharedAuditBody`. Oversized clamp remains unit-tested (`NavigationIntentsTest`).
- [x] Device app lock — **PASS** on `Medium_Phone_API_36.1`: enabling App lock showed `BiometricPrompt` (`DEVICE_CREDENTIAL`). After `am force-stop` + relaunch, focus was `BiometricPrompt` titled **Unlock app** (PIN field). PIN unlocked the library (`SHARE-AUDIT-TITLE` visible). App lock then turned off; emulator PIN cleared.
- [x] Device: internal nav token after force-stop — **PASS** on `Medium_Phone_API_36.1` (prefs + synthetic intent). Pixel 7: Glance **bound** (`AppWidgetId` 312); `run-as` SELinux denied so prefs were not re-read. Live tap after force-stop **PASS** (OPUS-AND-005): `flg=0x30000000` `notelikeus://editor/{id}` → `MainActivity` (sign-in gate; session not restored).
- [x] Device: App Functions OS invoke — **PASS** on Pixel 7 `31071FDH2007WT`: `listNotes` / `searchNotes` after debug reinstall. With widget on-screen, the dual-DataStore crash (OPUS-AND-004) reproduced then **PASS** after Koin used `settingsDataStore`. Lock-gated invoke **not run** (UI was the signed-out gate; device PIN not entered).
- [x] Device: Glance bind — **PASS** 2026-09-06 Pixel 7 Pixel Launcher: Widgets search → Notelikeus Widget 3×2 → **+ Add**. Bound id **312**. After AND-004, widget showed notes (not the fail-closed "App lock" placeholder).
- [ ] Device: SQLCipher after lock-screen credential reset (F26 path). PIN was set and later cleared this session; the guest library still opened, which matches Keystore keys **not** using `setUserAuthenticationRequired`. That is not the F26 invalidation path.
- [ ] Packaged Windows app: sign-in, DPAPI session survive restart, editor window, high DPI
- [ ] Real Chrome: LCP/CLS/INP, throttled 3G, 80% / 200% zoom, 320 / 390 / 768 / 1280 / 1920
- [ ] Theme sweep: System / Light / Dark / AMOLED × Neutral / Blue / Green, including note-color contrast
- [ ] `prefers-reduced-motion` in a real browser (CSS is present; not viewed)
- [ ] PWA install, update (`SKIP_WAITING`), offline reload, non-Latin font after latin-only precache
- [ ] Confirm production `ALLOWED_ORIGINS` includes the custom domain (CORS no longer allows all Pages). Staging probe **PASS** 2026-09-06: Worker version `51741604-9ecc-4b32-96ed-3b85d7ff86ad` allows `https://notelikeus-dev.pages.dev` and `https://dfd73f90.notelikeus-dev.pages.dev`; denies `https://evil.pages.dev`, `https://notelike.web.app`, `https://notelikeus.com`. No custom domain is attached in-repo.
- [x] Confirm Pages env `VITE_SUPABASE_URL` is `https://cqydlidescvmpfviwncf.supabase.co` (CSP is pinned to that host) — **PASS** 2026-09-06: hosted project `notelikeus-staging` / ref `cqydlidescvmpfviwncf` is ACTIVE_HEALTHY; that is the URL in `web/.env.staging` and Worker `wrangler.toml`.
- [x] Hosted OPUS-DATA-006 — **PASS** 2026-09-06: `fetch_full_snapshot` on `cqydlidescvmpfviwncf` is the scalar-subquery body; migration history `20260906000000 fetch_full_snapshot_scalar`.
- [x] Confirm Pages `VITE_ATTACHMENTS_WORKER_URL` matches the Worker origin that `pinAttachmentsCspPlugin` injects — **PASS** 2026-09-06: live `https://notelikeus-dev.pages.dev/` (and preview `https://dfd73f90.notelikeus-dev.pages.dev/`) CSP `connect-src` includes `https://notelikeus-attachments.error-endpoint.workers.dev` and does not include `https://*.workers.dev`. Index does not modulepreload `supabase-*.js`.
- [x] Hosted OPUS-PERF-004 — **PASS** 2026-09-06: `20260906010000 rls_initplan` on `cqydlidescvmpfviwncf`; performance advisors empty. Security advisor remaining: leaked-password protection disabled (Auth dashboard).
- [x] Favicon `sizes: any` in generated `web/dist/manifest.webmanifest` — **PASS** this continuation
- [ ] Play Console Data safety form vs rewritten `store/DATA_SAFETY.md`

---

## N. Prioritized backlog

### Block release

1. App Functions OS invoke **while App lock is on** (Pixel 7 has a device PIN; not toggled this session). SQLCipher after a Keystore-invalidating credential reset (not run on the personal phone).
2. If a production custom domain is attached, put it in Worker `ALLOWED_ORIGINS`. Staging Pages CORS is live-pinned (OPUS-SEC-003). Hosted schema OPUS-DATA-006 and OPUS-PERF-004 are on `notelikeus-staging` (`cqydlidescvmpfviwncf`).

### Fix next

1. Confirm the new `web.yml` `e2e` job is green on GitHub Actions (the same Playwright suite passed locally).
2. Enable Auth leaked-password protection on `notelikeus-staging` (HaveIBeenPwned; dashboard, not SQL).

### UX polish

1. OPUS-WEB-003 — `useCallback` the editor actions (optional; trap already fixed).
2. Verify reduced-motion visually; audit Compose animation specs the same way.
3. Desktop windowed editor: keyboard, snap, high-DPI (never run packaged here).

### Nice-to-have

1. OPUS-SEC-006 upload quota / rate limit.
2. Automated Lighthouse CI.
3. Gradle OSV/dependency scan in CI.

---

## Appendix — environment confirmation (this session)

```
docker: Docker Desktop 4.89.0, engine 29.7.2, WSL distro docker-desktop Running
pgTAP: 12 files / 81 tests PASS (incl. notelikeus_fetch_full_snapshot + notelikeus_rls_initplan)
playwright: 3/3 PASS (boot, signed-out gate, note create/reload/edit)
npm audit --omit=dev (web): 0 vulnerabilities (prior)
instrumented: composeApp 4/4, androidApp 2/2
share-target: editor opened with SHARE-AUDIT-TITLE / SharedAuditBody
app lock: BiometricPrompt "Unlock app" after force-stop; PIN unlock restored the library
widget token: prefs persist after force-stop; token+noteId=1 opened SHARE-AUDIT-TITLE; forged token did not
widget bind: Pixel 7 AppWidgetId 312 (Notelikeus Widget 3x2)
widget tap after force-stop: START MainActivity dat=notelikeus://editor/{id} flg=0x30000000 (AND-005)
app functions OS: listNotes + searchNotes PASS on Pixel 7 with widget bound (after OPUS-AND-004)
OPUS-AND-004: Koin uses settingsDataStore singleton
app functions OS: listNotes + searchNotes returned SHARE-AUDIT-TITLE id 1
AndroidSupabaseSessionPersistenceTest: 5/5
NavigationIntentsTest: widget NEW_TASK + URI fallback + legacy note-host URI
SettingsDataStorePathTest: 1/1
web npm test: 55 files / 366 tests
prod index.html: supabase chunk not modulepreloaded (live Pages too)
dist manifest: favicon sizes any
CSP live: pinned supabase project + attachments Worker origin; no *.workers.dev / *.supabase.co
Worker CORS live: notelikeus-dev.pages.dev allowed; evil.pages.dev denied
hosted advisors: performance empty after rls_initplan; security WARN leaked-password protection disabled
```
