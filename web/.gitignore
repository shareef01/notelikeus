node_modules
dist
dist-ssr
*.local
.env
.env.local
.DS_Store
dev-dist

# Playwright output. .env.e2e is NOT ignored — it is all placeholders and CI needs it.
test-results
playwright-report
blob-report
.playwright
